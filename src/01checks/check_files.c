#include "cub3d.h"

//to be used later, hidden for compilation

// static bool	is_png_file(char *file)
// {
// 	size_t	len;

// 	len = ft_strlen(file);
// 	if (len < 4)
// 		return (false);
// 	return (ft_strcmp(&file[len - 4], ".png") == 0);
// }
